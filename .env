JWT_SECRET=<YOUR JWT SECRET>

# Database Configuration (defaults to localhost for local development)
DB_HOST=localhost
DB_PORT=26257

# Redis Configuration (defaults to localhost for local development)
REDIS_HOST=auth-redis
REDIS_PORT=6379

# SMTP Configuration (optional, defaults to localhost)
SMTP_HOST=localhost
SMTP_PORT=1025

# Queue Configuration
QUEUE_HOST=auth-rabbitmq
QUEUE_PORT=5672